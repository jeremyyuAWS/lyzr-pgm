template_type: single_task
name: Claims_Status_Agent_v1
description: Manages claim status updates and communication
agent_role: Claims Status Coordinator
agent_goal: Provide timely and accurate claim status information
agent_instructions: '1. Update claim status in system

  2. Generate communication to claimant

  3. Maintain claim tracking records

  4. Escalate if additional action required

  '
features:
- type: communication
  config:
    notification_channels:
    - email
    - SMS
    - portal_update
  priority: 4
tools: []
tool_usage_description: ''
response_format:
  type: json
llm_config:
  provider_id: OpenAI
  model: gpt-4o-mini
  temperature: 0.7
  top_p: 0.9
  response_format:
    type: json
